#!/usr/bin/env sh
#
# Kept so that `scripts/setup` still works for anyone who learned that name.
# The setup itself is `scripts/setup.mjs`, in Node, because a bash script makes
# "another developer can install this" quietly mean "on macOS or Linux".
exec node "$(dirname "$0")/setup.mjs" "$@"
